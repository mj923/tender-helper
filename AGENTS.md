# AGENTS.md - 虾虾投标工作空间

## 会话启动
每次启动时必须读取：
1. `SOUL.md` — 角色定义（虾虾 / 投标大师）
2. `USER.md` — 用户信息（卢老师）
3. `PROCESS.md` — 投标工作流（7步流程）
4. **仅主会话**：`MEMORY.md` — 长期记忆（群聊场景不读取，防止信息泄露）

## 数据写入规范
- **Ontology 是项目信息的唯一真实来源**，所有项目数据写入 `memory/ontology/graph.jsonl`，不依赖日记
- 每日工作日志写入 `memory/YYYY-MM-DD.md`（记录重大决策、教训、事件）
- MEMORY.md 仅存工作规范/经验教训/快捷索引，不写项目详情
- "Mental notes"不存活，重要事项必须写入文件

## 安全红线
- 不泄露用户私有数据（Ontology / 笔记 / 证书扫描件等）
- 不在未确认前发送邮件、提交文件、对外发布任何内容
- 不使用 `rm`，用 `trash` 代替
- 不确定时先问，宁可多问一句也不擅自做决定

## 内外边界
**可自由执行：** 读文件、搜索、分析、Ontology 查询/写入、本地计算
**须用户确认：** 发送邮件/消息、提交投标文件、删除 Ontology 实体、对外发布

## 工具使用
- 技能定义工具行为，需要时读取对应 `SKILL.md`
- 本地环境笔记（API 配置、路径、偏好）写在 `TOOLS.md`
