# PROCESS.md - 虾虾投标工作流 V3

> **版本：** V3（通用信息化项目版）
> **更新日期：** 2026-06-07
> **适用范围：** 所有信息化类招标项目（医疗/信创/智慧城市/数据治理/运维服务等）
> **核心变更：** 第二步定位为纯分析阶段（无独立交付物）、输出物格式标准、目录索引定向解析

---

## 贯穿层：Ontology 与知识库机制

### Ontology 数据中枢

所有项目相关数据在 **每个步骤结束时** 统一录入 `memory/ontology/graph.jsonl`，形成"查→决策→写回"闭环。

| 步骤 | 录入内容 | Ontology 实体 |
|------|----------|--------------|
| 第一步 | 项目基础信息、招标文件、关键节点 | `BiddingProject` / `TenderDocument` / `Organization` / `Milestone` |
| 第二步 | 资格要求、评分标准、废标风险（中间表，汇入第三步） | `Requirement` / `ScoringCriteria` |
| 第二步（2.6） | 资质匹配结果 | 更新 `Requirement.our_status` + `evidence` |
| 第三步 | 分析报告产出（唯一正式交付物） | `BidDocument` |
| 第六步 | 投标计划任务 | `Task` / 追加 `Milestone` |
| 第七步 | 检查表记录 | `BidDocument` |

### 知识库现状与使用

| 知识库 | 当前状态 | 触发步骤 | 用法 |
|--------|----------|----------|------|
| 企业资质库 | ✅ 已建立（ima 笔记《集团企业资质清单》） | 2.6 | 自动比对企业资质与招标文件资格要求 |
| 人员库 | ⏳ 未建立 | — | 暂不参与匹配，人工核实 |
| 知识产权库（软著/专利） | ⏳ 未建立 | — | 暂不参与匹配，人工核实 |
| 业绩库 | ⏳ 未建立 | — | 暂不参与匹配，人工核实 |
| 废标案例库 | ⏳ 未建立 | — | 暂不参与匹配，人工核实 |

> **当前可用的自动匹配仅限企业资质库。** 人员、知识产权、业绩三类数据在建库完成后接入流程。

---

## 第一步：文件解析与预处理

1. 接收用户上传的招标文件（支持 doc/docx/pdf/ofd 格式）
2. 使用**目录索引+定向精读**方式解析：先识别文件目录/章节结构，建立章节→页码映射，再定向提取关键章节，按实际结构灵活调整解析范围
3. 输出：文件解析完成确认 + 核心文件基础信息（文件名称、格式、页数、核心章节分布、解析耗时）
4. **Ontology 录入：** 创建 `TenderDocument` 实体，记录 file_token / local_path / requirements_summary

---

## 第二步：招标文件全维度核心信息提取（分析阶段，无独立交付物）

> 本步骤为纯分析过程。2.1-2.6 的输出以结构化中间表形式汇总，统一作为第三步《招标文件分析报告》的输入，不产生独立交付物。

### 2.1 基础关键信息提取

**输入：** 招标文件全文（重点：投标邀请、投标须知前附表、资格审查、评标方法及标准章节）

| 提取维度 | 具体内容 |
|-----------|----------|
| 项目基础信息 | 项目名称、招标编号、采购人、代理机构、使用单位 |
| 核心规则信息 | 资金来源、招标方式、资格审查方式、最高投标限价、投标保证金金额与缴纳要求 |
| 时间与交付信息 | 实施周期、维保期、交付地点、投标截止时间、开标时间、投标有效期、投诉及质疑关键时间点 |
| 合同关键信息 | 履约周期、售后服务期限、付款条件、罚则 |

**分析要点：** 注意区分"预算""最高限价""投标报价"三者的关系；确认保证金是否收取、金额及缴纳方式；确认投标有效期是否满足90日最低要求。

**汇总到第三步：** 模块一 · 项目概况

### 2.2 投标人资格要求提取

**输入：** 招标文件"投标人资格要求"章节（含第一章投标邀请、第三章资格审查）

| 要求类别 | 提取要点 |
|-----------|----------|
| 基本资格 | 《政府采购法》第22条六项条件、投标人资格要求 |
| 特定资格 | 资质证书、人员配置、业绩要求、财务要求、知识产权、联合体要求 |
| 证明材料 | 证书复印件、合同复印件、社保证明、声明函等 |
| 否决项 | 实质性要求（★）、资格审查不合格情形 |

**分析要点：** 明确区分"资格性审查"（投标资格）和"符合性审查"（投标文件有效性）；标注哪些是★实质性要求，偏离即废标。

**汇总到第三步：** 模块二 · 投标人资格要求

### 2.3 项目建设 / 采购内容提取

**输入：** 招标文件"采购需求/技术要求"章节（第五章）

| 提取维度 | 具体内容 |
|-----------|----------|
| 采购标的 | 各包名称、数量、单价预算 |
| 技术参数 | ★实质性参数、▲重要参数、一般参数 |
| 功能需求 | 各子系统功能清单、性能指标 |
| 验收标准 | 初验/试运行/终验标准、软测/等保/密评要求 |
| 商务要求 | 报价要求、实施要求、售后服务、其他要求 |

**分析要点：** 区分"总体设计"和"详细技术参数"；识别国产化（信创）、等保、密评等刚性约束。

**汇总到第三步：** 模块三 · 项目建设/采购内容

### 2.4 评分标准与评审细则提取

**输入：** 招标文件"评标方法及标准"章节（第四章）

| 评分模块 | 提取要点 |
|-----------|----------|
| 价格分 | 价格分权重、基准价计算方式、异常低价审查触发条件、计算公式 |
| 商务分 | 各评审因素名称、分值、证明材料要求、得分核心逻辑 |
| 技术分 | 各评审因素名称、分值、评分要点、响应要求、得分逻辑 |

**分析要点：** 明确价格分计算公式；确认各评分项是否有★/▲标识；识别是否有"演示视频"要求及评分规则。

**汇总到第三步：** 模块四 · 评分标准与得分策略

### 2.5 废标 / 无效投标风险识别

**输入：** 招标文件全文（重点：投标须知、评标方法及标准、资格审查章节）

| 风险类别 | 识别要点 |
|-----------|----------|
| 废标情形 | 符合性审查不通过、实质性偏离、异常低价无法说明、不足三家 |
| 无效投标 | 报价超限、缺漏项、解密失败、偏离超标、选择性报价 |
| 资格审查不通过 | 资格证明缺失、信用记录不良、保证金/承诺书缺失 |
| 其他风险 | 串通投标、资料虚假、逾期递交等 |

**分析要点：** 对每一项风险标注触发条件、发生概率、规避建议；特别注意"解密时限""偏离总数阈值"等易忽略条款。

**汇总到第三步：** 模块五 · 废标风险与规避方案

### 2.6 投标能力匹配度分析（企业资质库匹配，必选）

**输入：** 2.2 资格要求清单 + 2.4 评分标准 + 企业资质库（ima 笔记《东软集团企业资质清单》）

| 匹配维度 | 当前状态 | 说明 |
|-----------|----------|------|
| 企业资质证书 | ✅ 自动匹配 | 比对招标文件要求的资质与我方证书 |
| 管理体系认证 | ✅ 自动匹配 | ISO 9001/20000/27001/14001/45001 等 |
| 人员证书 | ⏳ 待人工核实 | 项目经理/技术负责人/实施团队的软考证书及社保证明 |
| 业绩 | ⏳ 待人工核实 | 近三年信息化同类型项目业绩合同 |
| 知识产权 | ⏳ 待人工核实 | 软著/专利清单 |
| 信用记录 | ⏳ 待人工核实 | 信用中国/政府采购网查询 |

**分析要点：** 对自动匹配项给出得分预测；对人工核实项明确标注"待确认"并列出所需材料清单；识别短板项并给出补全建议。

**汇总到第三步：** 模块六 · 企业投标能力匹配分析

---

## 第三步：招标文件分析报告生成（唯一正式交付物）

1. 基于第二步 2.1-2.6 的所有分析成果，生成结构化、标准化的《招标文件分析报告》
2. 报告结构按以下 6 模块组织，覆盖第二步所有分析成果：
   - 模块一 · 项目概况（来自 2.1）
   - 模块二 · 投标人资格要求（来自 2.2）
   - 模块三 · 项目建设/采购内容（来自 2.3）
   - 模块四 · 评分标准与得分策略（来自 2.4）
   - 模块五 · 废标风险与规避方案（来自 2.5）
   - 模块六 · 企业投标能力匹配分析（来自 2.6）
3. 输出：完整的《招标文件分析报告》，支持 doc/docx 格式导出
4. **Ontology 录入：** 创建 `BidDocument` 实体，记录报告 doc_id / url / doc_type

**招标文件分析报告格式：**

| 报告模块 | 内容要求 | 表格/格式 |
|-----------|----------|-----------|
| 模块一 · 项目概况 | 项目背景、核心约束列表，≤300字 | 列表 |
| 模块二 · 投标人资格要求 | 表格：类别 \| 要求内容 \| 审查要点 \| 我方状态 | 表格 |
| 模块三 · 项目建设/采购内容 | 表格：序号 \| 一级分类 \| 二级分类 \| 建设内容摘要 \| 核心交付物 | 表格 |
| 模块四 · 评分标准与得分策略 | 主表格：评审大项 \| 子项 \| 满分 \| 计分标准 \| 得分策略 | 表格+列表 |
| 模块五 · 废标风险与规避方案 | 表格：R-001起 \| 风险等级 \| 风险描述 \| 规避措施 | 表格 |
| 模块六 · 企业投标能力匹配分析 | 匹配结果表格 + 优势/短板/补全建议 | 表格+列表 |


---

## 第四步：报告优化与终版确认

1. 主动询问用户是否有补充信息、是否对报告内容有调整需求、是否存在重大失误
2. 基于用户的反馈，对报告进行修正、补充、优化，确保报告 100% 贴合招标文件要求与用户实际需求
3. 输出：最终版《招标文件分析报告》，支持 doc/docx 格式导出

---

## 第五步：投标文件大纲生成

1. 严格基于招标文件中"投标文件格式"的强制要求，结合评分标准的核心得分点，生成完整、合规的投标文件大纲
2. 大纲必须完全匹配招标文件的格式要求，不遗漏任何强制章节，同时针对评分项设置对应的响应章节，确保得分点全覆盖
3. 输出：标准化的投标文件大纲，支持 doc/docx 格式导出，可直接用于投标文件编写
4. 支持用户手动调整大纲内容，适配企业实际投标需求

**投标文件大纲格式：**

- 使用多级编号章节目录（一、（一）、1.、（1））
- 每个章节标注：`对应评分项：xxx` / `强制要求：★` / `建议深化：▲`
- 章节末尾注明：`所需证明材料：xxx`
- 总页数预估标注在目录页

---

## 第六步：投标计划生成

1. 基于投标文件大纲的各子章节，识别招标文件中的所有关键时间节点（投标截止时间、开标时间、保证金缴纳截止时间、答疑截止时间等）
2. 以招标文件上传时间为计划开始时间，以投标文件递交截止日期前 1 天为计划结束时间，制定完整、可落地的投标工作计划
3. 计划必须包含：工作项、工作内容、责任人、开始时间、结束时间、交付物、前置依赖、优先级
4. 输出：完整的投标计划表，使用飞书电子表格
5. 支持用户手动调整计划内容，适配企业实际投标团队配置
6. **Ontology 录入：** 所有任务创建 `Task` 实体；关键节点追加 `Milestone` 实体

**投标计划表格式**：

**表格列名：** `序号 | 工作项 | 工作内容 | 责任人 | 开始时间 | 结束时间 | 交付物 | 前置依赖 | 优先级`

| 优先级 | `P0-关键路径` / `P1-高` / `P2-中` / `P3-低` |
|--------|--------------------------------------------------|
| 前置依赖 | 填写前置任务序号，无依赖填 `无` |
| 交付物 | 具体文件名称，格式为 `文件名.扩展名` |

---

## 第七步：投标文件检查表（新增）

> 本步骤在投标文件编写完成后执行（编写环节不在此流程范围内）。

1. 基于招标文件中的废标条款、实质性要求、关键技术参数，生成结构化的投标文件检查表
2. 检查表以电子表格（多维表格）形式输出，方便编写人员逐项勾选确认
3. 检查内容须覆盖：
   - 资格要求符合性（各承诺函/证书/证明是否齐全）
   - 实质性条款响应（★项是否逐条响应）
   - 唱标信封专项检查（金额一致性、签字盖章、文件顺序）
   - 格式要求（份数/封装/签字盖章/密封）
4. 输出：结构化投标文件检查表（飞书电子表格），编制人/复核人/授权人三级签字确认
5. **Ontology 录入：** 检查表产出物记录为 `BidDocument`，类型为 checklist
**投标文件检查表格式（飞书电子表格）**

**字段定义：**

| 字段名 | 字段类型 | 说明 |
|--------|----------|------|
| 检查项编号 | 文本 | C-001 起 |
| 检查类别 | 单选 | 资格要求/实质性条款/唱标信封/格式要求 |
| 检查内容 | 文本 | 具体检查描述 |
| 对应条款 | 文本 | 招标文件条款编号 |
| 检查结果 | 单选 | 通过/不通过/待确认 |
| 问题描述 | 文本 | 不通过时填写 |
| 编制人 | 人员 | |
| 复核人 | 人员 | |
| 授权人 | 人员 | |
| 检查日期 | 日期 | |
---

## 输出物格式标准

### 通用排版规范

- **标题层级**：H2 = 模块标题，H3 = 子项标题，H4 = 细节说明，不得跳级
- **表格强制要求**：必须有表头行；列数对齐；空单元格填充 `-`；超过 5 列考虑拆表
- **编号规则**：风险项 `R-001` 起，任务项 `T-001` 起，评分项 `S-001` 起，同一文档内编号不重复
- **文件头信息**：统一置于文档开头，格式为 `> 字段名：字段值` 的引用块，每行一个字段
- **日期格式**：统一使用 `YYYY-MM-DD`，时间使用 `YYYY-MM-DD HH:MM`（Asia/Shanghai）

---

## 执行规则补充

1. **严格执行格式标准**：以上表格列名、字段定义、编号规则为强制要求
2. **每步产出物须先过格式自检**：输出前核对是否符合对应模块输出格式
3. **不确定时先问**：格式不明确的优先询问用户，不自行假设
4. **各步骤产出物统一存入 Ontology 和飞书云空间**，确保可追溯
5. **知识库未建立的人员/知识产权/业绩维度**：在分析报告中明确标注 `待人工核实`
6. **分析报告固定 6 模块**，每模块必须有对应的表格或列表，不得纯段落输出

---

## Ontology 写入强制规范

### 写入时机（强制检查点）

每个步骤结束时，必须先完成 Ontology 录入才能进入下一步。每步检查点如下：

| 步骤 | 必须录入 | 检查方法 |
|------|----------|----------|
| 第一步 | TenderDocument + Organization（采购人/代理） + Milestone（开标/截止） | 查询 graph.jsonl 确认存在 |
| 第二步 | Requirement（资格要求条目）+ ScoringCriteria（评分标准条目） | 查询 graph.jsonl 确认数量≥N |
| 第三步 | BidDocument（分析报告 doc_id + url） | 查询确认已写入 |
| 第六步 | Task（每项工作）+ Milestone（关键节点） | 查询确认数量匹配 |
| 第七步 | BidDocument（检查表 doc_id + url） | 查询确认已写入 |

### 实体写入标准格式

所有实体必须使用以下 JSON 格式写入 `memory/ontology/graph.jsonl`，每行一个 JSON 对象：

```json
{"op":"create","entity":{"id":"<实体ID>","type":"<实体类型>","properties":{...}}}
```

不推荐使用扁平格式，会导致协议不一致。

### 实体 ID 命名规范

| 实体类型 | ID 前缀 | 示例 |
|-----------|---------|------|
| BiddingProject | `proj_` | `proj_hunan_medical_platform` |
| TenderDocument | `tender_` | `tender_hunan_2026` |
| BidDocument | `doc_` | `doc_hunan_analysis` |
| Organization | `org_` | `org_hunan_bureau` |
| Requirement | `req_` | `req_hunan_basic_01` |
| ScoringCriteria | `score_` | `score_hunan_pkg1_S001` |
| Milestone | `milestone_` | `milestone_hunan_opening` |
| Task | `task_` | `task_hunan_001` |
| Event | `event_` | `event_hunan_opening` |

### 每步完成后的验证命令

```bash
grep "<project_id>" memory/ontology/graph.jsonl | wc -l
```

预期输出：第一步≥6，第二步≥20，第三步≥1，第六步≥25，第七步≥1。

### 优先级定义

- **P0-必须写入**：BiddingProject、TenderDocument、Organization（采购人/代理）、BidDocument（分析报告）、Task（关键任务）、Milestone（关键节点）
- **P1-建议写入**：Requirement（全部资格要求）、ScoringCriteria（全部评分标准）、Milestone（全部时间节点）
- **P2-可选写入**：Event、部分非关键 Task

### 格式自检清单（每步结束前核对）

- [ ] 实体 ID 是否符合命名规范
- [ ] 是否使用标准 `op/entity/properties` 格式
- [ ] 必填字段是否齐全（project_id / name / type）
- [ ] 是否存在与现有实体重复的 ID
- [ ] 是否已写入 graph.jsonl 并通过验证命令确认

---

## 解析效率规范

### 解析策略

1. **目录索引先行**：识别文件目录/章节结构，建立章节→页码/位置映射
2. **定向精读**：根据映射表提取关键章节，跳过设备参数表、格式模板等低频价值页
3. **灵活调整**：根据文件实际结构确定解析范围，无固定页数限制
4. **精度要求**：章节起始位置定位准确，确保关键内容不遗漏

### 关键章节（参考）

不同招标文件的章节顺序和命名可能不同，以下列示典型关键章节及其通常包含的核心内容，供解析时参考定位：

| 章节名称 | 通常包含的核心内容 |
|----------|-------------------|
| 投标邀请 / 公告 | 项目名称、预算、开标时间、资格要求 |
| 投标人须知前附表 | 规则汇总（预算、保证金、评分方法、投标文件份数） |
| 资格审查 | 资格性审查标准、证明材料要求 |
| 评标方法及标准 | 评分标准、符合性审查、废标条款 |
| 采购需求 / 技术要求 | 技术参数、功能需求、商务要求 |
| 合同条款 | 履约要求、付款条件、违约责任 |
| 投标文件格式 | 投标文件组成、强制章节清单、签署盖章要求 |

> **注意：** 具体章节顺序和名称以实际招标文件为准，灵活判断。